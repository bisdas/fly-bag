import Content from '../constants/content';
// import { getCollectionByName } from '../firebase/collectionUtils';
import Settings from '../constants/settings';

export const getAllCategories = () => {
    return []; //getCollectionByName(Settings.firebaseCollections.categories);
}